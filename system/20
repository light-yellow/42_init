compgen -u
cut -d: -f1 /etc/passwd

# compgen - встроенная утилита bash, которая принимает на вход список всех возможных значений аргумента,
# а так же текущую введенную часть аргумента, и выбирает те значения, до которых введенную часть можно дополнить. 
