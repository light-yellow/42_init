cat /var/run/sshd.pid

# SSHD значит "SSH Daemon" или "SSH Server"
# SSHD это демон, который ожидает соединения от клиентов и предоставляют защищенную и зашифрованную связь между двумя не доверенными компьютерами через незащищенную сеть.
# PID (process identifier - Идентификатор процесса) — уникальный идентификатор процесса в многозадачной операционной системе.
