systemctl list-units --type service --all
systemctl list-unit-files | grep enabled
systemctl list-unit-files --type=service --state=enabled

# systemd - система инициализации, которая запускает все необходимые демоны, фоновые процессы и службы, 
# которые требуются для корректного функционирования операционной системы.
# Главная команда для отслеживания и контроля состояния systemd - команда systemctl.
#ls /etc/init.d/
