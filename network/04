route get default | grep gateway | awk '{print $2}'

# Routing table (таблица маршрутизации) - таблица, хранящаяся на маршрутизаторе или сетевом компьютере,
# которая описывает соответствие между адресами назначения и интерфейсами, 
# через которые следует отправить пакет данных до следующего маршрутизатора.
# Default gateway (шлюз по умолчанию) - сетевой шлюз, на который пакет отправляется в том случае,
# если маршрут к сети назначения пакета не известен.
# route - просмотр/изменение таблицы IP-маршрутизации
