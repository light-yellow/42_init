ifconfig -l

# network interface - сетевая плата, также известная как сетевая карта, сетевой адаптер, Ethernet-адаптер — дополнительное устройство,
# позволяющее компьютеру взаимодействовать с другими устройствами сети.
# ifconfig (сокр. interface configuration):
# Используется для вывода информации и конфигурирования параметров сетевого интерфейса.
# -l: Вывести список всех доступных интерфейсов в системе без дополнительной информации.
