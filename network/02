broadcast_address=$(ifconfig en0 | grep 'broadcast' | awk '{print $6}')
echo "Broadcast address:"
echo $broadcast_address
echo "IP adresses in the same subnet:"
ping -c 2 $broadcast_address > /dev/null
arp -a | awk -F "[()]" '{print $2}'

# PING означает Packet InterNet Groper. Утилита или программа для определения того, доступен ли конкретный IP-адрес
# из вашей сети или хост / сервер. Утилита Ping обычно используется для проверки сетевых ошибок и диагностики этой проблемы.
# Механизм работы прост, он отправляет пакет на указанный IP-адрес /хост /адрес сервера, ожидает ответа от этого хоста
# и измеряет время ответа. Это также называется задержкой.
# Когда мы делаем пинг broadcast адреса, команда отправляется на все устройства в заданной сети. Поэтому при пинге по нему, 
# мы получим ответ от всех, и "запомним" про них информацию.
# ARP (Address Resolution Protocol - протокол определения адреса) — протокол, предназначенный для преобразования IP-адресов в MAC-адреса. 
# ARP-таблица отображает IP и MAC подключенных к серверу сетевых устройств.
# arp -a отображает содержимое таблицы.
